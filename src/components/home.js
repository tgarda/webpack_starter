import React from 'react';

import styles from '../../style/home.css';

const Home = (props) => (
  // <h1 className='home' className={styles.home}>
  <h1 className={styles.header}>
    Welcome Home!!!!
  </h1>
)

export default Home;
